<소켓>
서버와 클라이언트는 각각 소켓 생성

socket()

bind() => 생성된 소켓에 주소 부여
127.0.0.1:7777

listen() =>127.0.0.1:7777
실질적으로 서버를 연다.

accept() => 클라이언트의 요청
클라이언트가 처음연결하려는 connect을 대기
도킹 대기모드 - 도킹요청이 들어올때만 열어준다

---서버 끝 / 클라이언트 시작---

connect() => 서버로 연결하기 위한

send() =>  메시지 전송
recv() => 메시지 받을때

콘솔창이 닫힌다.

close()



SOCKET socket(int af, int type, int protocol)

int bind(SOCKET s, const sockaddr* name, int namelen)

s 클라이언트의 연결을 기다리는 소켓 객체 socket()로 생성된 소켓 객체



1. 회원가입
입력 아이디 페스워드 등등
아이디 회원구별 기본키
아이디 - 중복처리 후에 중복일경우 다시입력하세요 출력

2. 로그인 mySQL (id pw)
소켓 실행 id pw 일치하는지 확인 안될경우 잘못된입력입니다.

3. 채팅 => 채팅정보도 db에 저장한다.  
-id를 외래키로 받아온다.
3-1 다시 입장했을때 채팅내역을 출력해준다

번외 dm구현
