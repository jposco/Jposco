#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
	//-----------------벡터에 대해서---------------------------------------------
	//vector<int> v; //****기본적인 벡터함수****
	//v.assign(4, 5); //크기가 5인 배열, 모든 값을 2로 초기화
	////{5, 5, 5, 5}
	//v.push_back(1);	//v.push_back(i) 백터의 마지막에 원소를 넣는다.
	////{5, 5, 5, 1}
	////v.size(); // 벡터의 사이즈를 변환한다 (for문에 사용)
	//for (int i = 0; i < v.size(); i++)//for문에 적용하기
	//{
	//	cout << v.at(i) << " ";; //cout << v; 는 오류발생 확률이 있다.
	//	//{5, 5, 5, 1}
	//}
	//v.pop_back(); //벡터의 마지막 원소를 삭제한다.
	//{5, 5, 5}
	//v.begin();    //사용시 벡터의 시작주소를 반환함.
	//5
	//v.insert(v.begin() + 1, 0); //(넣고 싶은 자리, 넣고 싶은 숫자) : *추가시킴
	//{5, 0, 5}
	//v.erase(v.begin(), v.begin() + 1); //삭제하고 싶은 범위를 삭제함.
	//{5}
	//v.clear();   //벡터 제거.
	//v.front(); //첫번째 벡터 접근.









}